# 🧹 parser_agent.py: HTML → 구조화 데이터 파싱
 
def parse_bean_html(html: str, brand: str):
    # TODO: HTML을 파싱해서 { name, price, flavor, url, ... } 형태로 변환
    # brand에 따라 파싱 방식 분기 가능
    # return 구조화된 원두 데이터 배열
    pass 